# LISA agent

POSIX activity-simulation agent. One codebase for **Linux and macOS**; the only
platform-specific bits live in `lisa_agent/platform_ops.py`.

```bash
pip install -e '.[dev]'
lisa-agent          # run
pytest              # test
```
